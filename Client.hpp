#ifndef CLIENT_HPP
#define CLIENT_HPP

#include <>

class Client{
    private:
        std::string nickname;
        std::string username;
        std::string realname;
        bool isRegistered;
        bool passVerified;
    public:

};

#endif